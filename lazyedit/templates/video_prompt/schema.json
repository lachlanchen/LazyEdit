{
  "type": "object",
  "properties": {
    "prompt": {
      "type": "string",
      "description": "Final production-ready video prompt"
    },
    "negative_prompt": {
      "type": "string",
      "description": "Short exclusions list"
    },
    "model": {
      "type": "string",
      "enum": ["sora-2", "sora-2-pro"]
    },
    "size": {
      "type": "string",
      "enum": ["1280x720", "1920x1080", "1024x576", "720x1280", "1080x1920"]
    },
    "seconds": {
      "type": "integer",
      "minimum": 4,
      "maximum": 20
    }
  },
  "required": ["prompt", "model", "size", "seconds"],
  "additionalProperties": false
}
