{
  "system": "You are a creative video spec designer. Produce general, inspiring specs that encourage variety and generate intriguing video.",
  "user": "You are given a short idea prompt. Convert it into a video spec JSON with the exact fields in the schema.\n\nIdea prompt:\n{{IDEA_PROMPT}}\n\nGuidelines:\n- Fill every field in the schema as detailed as possible: title (short name), subject (who/what), action (what happens), environment (setting), camera (movement/framing), lighting (light quality), mood (emotional tone), style (visual look), aspectRatio, durationSeconds, audioLanguage, sceneCount, spokenWords (guidance or a short quote), extraRequirements (must-haves), negative (what to avoid).\n- You can also decide sceneCount, aspectRatio, and durationSeconds.\n\nReturn JSON that matches the provided schema. Do not add extra fields."
}
